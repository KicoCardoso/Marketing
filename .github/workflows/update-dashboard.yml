name: Atualizar dashboard de marketing

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Buscar dados do ClickUp
        env:
          CLICKUP_TOKEN: ${{ secrets.CLICKUP_API_TOKEN }}
        run: node scripts/fetch_clickup.mjs

      - name: Buscar dados do Relatorio (Meta Ads + funil)
        env:
          META_ADS_ACCESS_TOKEN: ${{ secrets.META_ADS_ACCESS_TOKEN }}
          CLICKUP_TOKEN: ${{ secrets.CLICKUP_API_TOKEN }}
        run: node scripts/fetch_report_data.mjs
        continue-on-error: true

      - name: Publicar alterações
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data.json
          if [ -f docs/report_data.json ]; then git add docs/report_data.json; fi
          git diff --cached --quiet || git commit -m "chore: atualizar dados do dashboard"
          git push
